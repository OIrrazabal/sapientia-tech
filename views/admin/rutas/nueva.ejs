<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Sapientia Tech Courses - Nueva Ruta de Aprendizaje</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">

    <!-- Favicon -->
    <link href="/assets/img/favicon.ico" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="/lib/animate/animate.min.css" rel="stylesheet">
    <link href="/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">

    <!-- Bootstrap & Template Stylesheet -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>

<body>
    <%- include('../../partials/navbar', { usuario: usuario, appName: appName }) %>

    <div class="container-fluid bg-primary py-5 mb-5 page-header">
        <div class="container py-5">
            <div class="row justify-content-center">
                <div class="col-lg-10 text-center">
                    <h1 class="display-3 text-white animated slideInDown">Nueva Ruta de Aprendizaje</h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb justify-content-center">
                            <li class="breadcrumb-item"><a class="text-white" href="/admin/home">Inicio</a></li>
                            <li class="breadcrumb-item"><a class="text-white" href="/admin/rutas">Rutas</a></li>
                            <li class="breadcrumb-item text-white active" aria-current="page">Nueva Ruta</li>
                        </ol>
                    </nav>
                </div>
            </div>
        </div>
    </div>

<div class="container">    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <a href="/admin/rutas" class="btn btn-outline-secondary px-3 py-2 rounded-pill">
                    <i class="fa fa-arrow-left me-2"></i> Volver al listado
                </a>
            </div>
            <h2 class="text-center">Crear Nueva Ruta de Aprendizaje</h2>
        </div>
    </div><% if (mensaje) { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= mensaje %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <% } %>
    
    <% if (error) { %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    <% } %>

    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-body">                    <form action="/admin/rutas/crear" method="POST" id="formCrearRuta">
                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre <span class="text-danger">*</span></label>
                            <input type="text" 
                                   class="form-control" 
                                   id="nombre" 
                                   name="nombre" 
                                   value="<%= typeof datos !== 'undefined' && datos.nombre ? datos.nombre : '' %>"
                                   required>
                            <div class="form-text">Ingresa un nombre para la ruta de aprendizaje.</div>
                        </div>

                        <div class="mb-3">
                            <label for="descripcion" class="form-label">Descripción</label>                            <textarea class="form-control" 
                                      id="descripcion" 
                                      name="descripcion" 
                                      rows="4"><%= typeof datos !== 'undefined' && datos.descripcion ? datos.descripcion : '' %></textarea>
                            <div class="form-text">Describe brevemente el objetivo y contenido de esta ruta de aprendizaje.</div>
                        </div>

                        <div class="mb-3 text-center">
                            <p>Después de crear la ruta, podrás agregar cursos desde la pantalla de edición.</p>
                        </div>                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg py-3 shadow" id="btnGuardarRuta">
                                <i class="fa fa-save me-2"></i> Guardar Ruta
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

    <%- include('../../partials/footer') %>

    <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/lib/wow/wow.min.js"></script>
    <script src="/lib/easing/easing.min.js"></script>
    <script src="/lib/waypoints/waypoints.min.js"></script>
    <script src="/lib/owlcarousel/owl.carousel.min.js"></script>
    <script src="/js/main.js"></script>
      <script>
        // Script para manejar el formulario de creación de ruta
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.getElementById('formCrearRuta');
            let isSubmitting = false;
            
            if (form) {
                form.addEventListener('submit', function(e) {
                    // Validar el nombre
                    const nombreInput = document.getElementById('nombre');
                    if (!nombreInput.value.trim()) {
                        e.preventDefault();
                        alert('Por favor ingresa un nombre para la ruta de aprendizaje');
                        return;
                    }
                    
                    // Prevenir envíos duplicados
                    if (isSubmitting) {
                        e.preventDefault();
                        return;
                    }
                    
                    // Marcar como enviando y deshabilitar botón
                    isSubmitting = true;
                    const submitBtn = document.getElementById('btnGuardarRuta');
                    if (submitBtn) {
                        submitBtn.disabled = true;
                        submitBtn.innerHTML = '<i class="fa fa-spinner fa-spin"></i> Guardando...';
                    }
                });
            }
            
            // Limpiar variables de sesión automáticamente después de 5 segundos
            // para evitar que se muestren mensajes antiguos
            setTimeout(function() {
                const alerts = document.querySelectorAll('.alert');
                alerts.forEach(function(alert) {
                    if (alert) {
                        var bsAlert = new bootstrap.Alert(alert);
                        bsAlert.close();
                    }
                });
            }, 5000);
        });
    </script>
</body>
</html>
